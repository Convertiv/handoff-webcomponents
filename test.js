const test = `<div>
    <h1>${title}</h1>
${show ??         <p>Nothing to show</p>
 }}
${show ?         <p>${description}</p>
 :         <p>Nothing to show</p>
}
${items.map((item) =>         <p>${this}</p>
)}</div>`;